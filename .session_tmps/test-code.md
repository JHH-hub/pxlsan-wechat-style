# 测试：代码块与新特性

> 这是一段引用，用来测试样式。

## 代码块测试

下面是一个 JavaScript 代码块：

```javascript
const greeting = 'Hello, Pxlsan!';
function render(msg) {
  console.log(`[Pxlsan] ${msg}`);
}
render(greeting);
```

还有一个无语言标注的代码块：

```
npm install
node scripts/render.cjs article.md
```

## 内联格式测试

这里有 **加粗文字**、*斜体文字*、~~删除线~~，还有 `行内代码`。

## 图片 alt 测试

![这是 <带尖括号> 的 alt 文字](https://via.placeholder.com/400x200)
